interface:
  display_name: "Radar Simulation"
  short_description: "Run Selena simulations with automatic MCP bootstrap"
  default_prompt: "Use $radar-sim-simulation to run the requested data, recover the last configuration when possible, and return the verified simulation result address."
