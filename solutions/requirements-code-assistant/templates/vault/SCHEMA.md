---
title: Requirement schema
type: schema
---

# Requirement schema

Store one independently testable requirement per note under `Requirements/Atomic`.

Required status values:

- Review: `draft`, `reviewed`, `approved`, `rejected`
- Implementation: `unknown`, `candidate`, `matched`, `mismatch`, `missing`
- Verification: `unverified`, `partial`, `verified`, `failed`

